# 2026-08-29 工作日志

## 白天模式深色背景二次修复 (commit a09d36f)

**用户报告**："弹窗样式恢复了，但社交媒体分发抽屉 / 算力中心调度策略卡片白天还是深色，是否全面排查恢复？" —— 附两张截图。

**根因查清（不在我之前修改范围）**：
- 全扫发现：`dashboard.tokens.css` 把 `--white-01` ~ `--white-40`（11 档）在白天主题下反转成 `rgba(0,0,0,0.NN)` 黑色透明；叠加在浅白玻璃面板 = "浅色面板上糊一层黑色透明"，视觉深色。
- 全仓库 34 个 dashboard CSS 文件里有 **134 处** background 用 `--white-NN` 且无 light 覆盖，分布在 13 个文件。
- 这些 `--white-NN` 不是我引入的——是 `3bee6b8`（origin/main）基线就有的设计系统未完成主题适配。

**用户决策**：仅修他截图的两处（dispatch.css + compute.strategy.css），不扩大范围。

**修复细节**：
- `dispatch.css`：尾部追加 `[data-theme="light"] [PATCH]` 块覆盖 10 个选择器（滚动条、matrix-item、t-pod、audit-badge、hub-btn、lab-box、m-tokens、lab-status-row .badge、locale-code-badge）；原 light 块内 5 处 `var(--white-NN)` 替换为 `rgba(255,255,255, .NN)` 字面。
- `compute.strategy.css`：尾部追加 `[PATCH]` 覆盖 `#view-compute .strategy-item` 与 `:hover`。
- 钩子验证：`✅ All checks passed`。`rgba(255,255,255, X)` 在 line 38 "允许纯白与纯黑半透明因子" pass；`rgba(15,23,42, X)` 与 `#0f172a` 不在 hex regex 内。

**保留给后续**：剩下 11 个文件 122 处 background 白天反转未修（用户未授权扩大范围），包括 `glass.css` / `glass.editor.css` / `glass.layout.css` / `glass.sovereign.css` / `glass.widgets.css` / `compute.css` / `compute.header.css` / `dashboard.components.css` / `modals.forms.css` / `modals.alerts.css` / `review.drawer.css` / `governance.css` 等。如果用户后续报告某处白天仍深色，可按同根因同解法追加。

**未来教训**：当用户报告"白天 X 还是深色"，**务必先全扫 token 反转影响范围并报告**，再问修复范围——上轮就是因为没扫全、用小样本判断，误以为只 7 个文件有问题。

## 白天分发抽屉 + 算力卡片精修 (commit e7cc5da)

**用户再次截图**："实时广播卡片视觉预览浅了，其他没变化"。分析发现前两轮 PATCH 改错地方：
- 图1 抽屉实际类名 = `.syndicate-drawer-overlay`（创建于 `syndicate.render.js:117`），不是 `.dispatch-hub-panel`。syndicate 抽屉用 inline `style.cssText` 硬编码 `rgba(15,17,26,0.96)`，CSS 需 `!important` 才能压过 inline style。
- 图2 调度卡片实际类名 = `.binding-terminal(.primary/.fallback)` + `.strategy-command-deck` + `.concurrency-guide-deck` + `.guide-step-card`，不是上一轮猜的 `.strategy-item`（根本不存在）。真根因：`.binding-terminal` 用 `var(--bg-card-rgb, 15, 23, 42)` 但 `--bg-card-rgb` 在 tokens.css 未定义，**fallback 永远深蓝灰**。

**本次两文件补丁**：
- `theme.light.overrides.css:609+` 加 `[data-theme="light"] .syndicate-drawer-overlay { background: rgba(255,255,255,0.96) !important; ... }` 强覆盖 inline
- `compute.strategy.css:488+` 重写 PATCH 用真实选择器

**未来教训（强化）**：当用户截图指错位置，**不能再凭截图关键字猜选择器**，必须：
1. 读 JS render 文件确认实际 `className`
2. 检查目标元素是否使用 inline style.cssText 硬编码背景
3. 验证目标 CSS 变量是否真的在 tokens.css 定义（避免被 fallback 误导）

## 用户开启浏览器实地排查 + 发现新 bug

**用户启动服务 `http://127.0.0.1:43212/dashboard/`，要求我用浏览器自动化直接测试界面问题**。

**加载 `agent-browser` skill**（按规范必须），装到托管 node 工作区（不走全局），Chromium 152 下载 179MB 一次完成。

**核心新发现：分发抽屉"当前选择语种"样式在两种模式下都丢失**：
- 整个 CSS 树里**零条 `.lang-radio-btn` 规则**（`grep -rn "lang-radio-btn" web/dashboard/css/` 无结果）
- `syndicate.render.js:151` 把 active/inactive 区分样式砍掉了，只剩 `padding/border-radius/font-size/cursor/display/gap`，没有 `background` / `border-color` / `color`
- 证据文件 `.plenipes/governance/evidence/article_syndicate_drawer.html.dump:21,25` 显示原设计：active=`rgba(0,242,255,0.15)` 青底+accent 边，inactive=`rgba(255,255,255,0.02)` 极淡白底
- 实测 dark 模式：3 个 lang 按钮 bg/color/border 完全相同（都是透明+白字）
- 实测 light 模式：抽屉白底（我之前 e7cc5da 修好了），但 3 个按钮依然无区分
- 这是 `syndicate.render.js` 重构时的回归

**全面排查 vault 页 + 算力中心 light 模式发现的问题**：
1. **文件表行高崩溃**：`vault.list.js:128` 操作按钮容器写死 `flex-direction: column`，8 个 emoji 按钮竖排，行高 174-231px（应 60-70px）。列宽实际 133/213/90/199 与 th 声明 35%/auto/60/120 不匹配。
2. **vault-drawer（实时广播 hub）离屏**：`position:fixed; right:-480px` 在 x=1320-1760（视口 1280），33 个子元素全部离屏。关闭态抽屉，**但页面上找不到可见的触发按钮**。
3. **令牌反转实测确认**：`--color-white: #000000`、`--color-black: #ffffff`、`--bg-card-rgb: ""`（空）。与之前分析一致。
4. **算力中心 light 模式 e7cc5da 修复有效**：`.strategy-command-deck` 浅玻璃 `rgba(255,255,255,0.75)`、`.guide-step-card` 浅色、`.concurrency-guide-deck` 青色浅玻璃，✅ 完全正常。

**给用户报告了 4 个发现，未修复（等用户决策）**。这是关键教训：**现在能直接打开浏览器验证，不再需要用户反复截图传回**。

## agent-browser 安装位置（避免污染用户环境）

按 SOP 隔离规范，agent-browser 装到 `/Users/eason/.workbuddy/binaries/node/workspace/node_modules/.bin/agent-browser`，Chromium 落到 `/Users/eason/.agent-browser/browsers/chrome-152.0.7977.64`。PATH 必须同时带 `node/versions/22.22.2/bin`（让 `node` 是可执行的，不是只打印版本）和 `node/workspace/node_modules/.bin`（找到 agent-browser）。每个 Bash 子 shell 都要重 export。

**关键调用模板**：
- 打开：`agent-browser open <url>` + `agent-browser wait --load networkidle`
- 触发 `openArticleSyndicationDrawer(rel, title)` 之类的 async 抽屉：eval 调完后 `await new Promise(r=>setTimeout(r, 600))` 等滑入（render.js 用 setTimeout 10ms + transition 0.35s）
- 强制兜底打开：`const d=document.getElementById('xxx-drawer'); if(d) setTimeout(()=>{d.style.right='0px';},20);`（绕过前置条件）
- 查 drawer 类名/结构：`grep -rn "id=\"xxx-drawer\"\|xxx-drawer-overlay\|class.*xxx-panel" web/dashboard/js/ui/`
- 查 drawer 打开函数：`grep -rn "openXxxDrawer\s*=\|window\.openXxxDrawer" web/dashboard/js/`

## vault-drawer 真实用途（不是"实时广播"）

**用户问实时广播是什么，浏览器实际打开后确认**：

- **标题是「🌐 网页托管发布」**，class 是 `dispatch-hub-panel`，内容三段：
  1. **GLOBAL SYNC MATRIX**（多语种装帧产物矩阵，每语种卡片显示装帧时间/tokens/段落缓存进度，含"实时预览"按钮）
  2. **ASSET TELEMETRY & AUDIT**（累计成本 ACCUMULATED COST、算力节点 COMPUTE CORE、审计状态）
  3. **LIVE PREVIEW ENGINE**（本地实时预览引擎，状态 OFFLINE/在线，"🔌 启动实时预览引擎"按钮）
- 底部三个动作：开始全站托管发布 🚀 / 强制重译 🧹 / 物理销毁 🗑️
- **功能 = 把当前原稿翻译、装帧成多语种静态网站**，通过 GLOBAL SYNC MATRIX 里列出的托管通道一键发布
- 我之前一直叫它"实时广播 hub"是**误称**——class 名 `dispatch-hub-panel` 意为"分发中枢"，但实际是网页托管，不是社媒广播（社媒广播是 `syndicate-drawer-overlay`）。drawers.js:11 inline 写了"Dispatch Hub Backdrop Overlay (与社交广播一致的轻量半透明毛玻璃遮罩)"，是遮罩文案让我混淆。

**vault-drawer 真实 UI 入口（用户指出后实测确认 ✅）**：
- **原稿文库每行操作栏下排第 2 个 🌐 按钮**（`vault.list.js:139` `onclick="openVaultDrawer('${m.rel_path}')"`，title「网页托管发布与全网遥测 (Web Hosting & Publish)」）。8 个按钮布局：上排 📝📤👁️📋 / 下排 🌍🌐📢🗑️（外层 `.vault-actions-grid` 是 `flex-direction:column` 让两排上下叠，每排内层是横排 flex）。
- 实测：点这个 🌐 → `vault-drawer` 从 `right:-480px` 滑入 `0px`，`docId` 正确显示。入口**完全可用**。
- **我之前排查时的误判**：在报告里写"vault-drawer 离屏、页面上找不到可见触发按钮"是错的。当时注意力全在抽屉关闭态离屏（right:-480px）和搜 drawer DOM 结构，漏看了文件行操作栏的 🌐 按钮。教训：**找抽屉入口必须扫操作栏按钮的 `onclick`，不能只搜 drawer 类名/结构**。
- 撤回排查报告"问题 3"，它不是 bug。

**独立 bug（与入口无关，仍存在）**：sidebar「阶段6 社交平台同步」按钮 `openDispatchHub` 是 undefined，点了走 else 跳 plugins 视图。那是该按钮命名/链接问题，不影响 vault-drawer 的 🌐 入口可用性。

**视觉**：
- light 模式 vault-drawer 背景仍是 `rgba(15,17,26,0.96)` 深色（drawers.js:11 inline 硬编码），但**视觉上没问题**（抽屉独立深色 + 页面浅色形成对比，drawer 内 GLOBAL SYNC MATRIX 卡片是浅色 token 渲染）。抽屉设计意图是"独立于页面主题的固定深色舞台"。如果用户后续报告 vault-drawer 在 light 模式要变浅，需在 theme.light.overrides.css 追加 PATCH（类比之前 syndicate 抽屉修复）。

**教训**：当用户问"X 是做什么的"，**先读 drawer 的 h3 标题和 section header 确认真实功能**，不能凭 class 名（dispatch-hub）或文件位置（drawers.js）猜。截图+读 DOM innerText 是最快方式。




---

**PyYAML 已装进托管 python（钩子环境修复）**：
- 钩子 `.githooks/pre-commit` 用 `#!/usr/bin/env python3`，git 触发时 PATH 上第一个 python3 = 托管 python `/Users/eason/.workbuddy/binaries/python/versions/3.13.12/bin/python3`，它此前缺 PyYAML → `exemption_loader.load_redline_exemptions()` 静默降级空集合（豁免白名单失效，>300 行 WIP 会被红线拦截）。
- 已 `$PY -m pip install pyyaml`（PyYAML 6.0.3）。验证：`load_redline_exemptions()` 现正确解析出 56 个豁免文件，提交时钩子不再打印「YAML 解析失败」告警。
- **注意**：装的是托管 python 的 site-packages 而非隔离 venv（因钩子直接走裸 `python3`，venv 不被钩子使用）。若未来重装/重置托管 python，需重新安装 PyYAML。

**今日提交汇总**：
- `92086af` chore(dashboard): 提交预览完成页「下一步建议」卡片 + index.html 资源版本号 v82→v83（2 files, +12/-4）
- 此前：`a9b0b71` 修复 lang-radio-btn active 态；`10b467e` memory 日志

---

**全项目架构扫描（3 路 Explore 并行 + 抽查核实）**：

- **服务框架更正**：`services/api/server.py` 是 **FastAPI**（非 Flask），挂 6 组 router，dashboard 静态挂载于 `/dashboard`（server.py:94-96），API 端口 43212、预览 43213。
- **主数据流**（已核实函数链）：`plenipes.py` → scanner.build_task_queue → `core/runtime/orchestration/sync_worker.py:perform_sync` → editorial Pipeline（normalize→purify→masking→metadata→seo→verification→semantic_linker）→ `core/logic/ai/ai_scheduler.py:33 dispatch_targets`（29 家 AI 提供商，adapters/compute）→ `core/bindery/bindery_dispatcher.py:40 dispatch`（解蔽+SSG 渲染，7 套主题）→ IncrementalBuildManager.build → `core/syndication/publisher.py:45 run_syndication`（egress：11 家静态托管 + 19 社媒渠道 + 12 图床）→ archives/ledger SQLite 记账。
- **前端**：179 个 JS 模块（33629 行）"Hub Facade + shards"组织；7 合法视图（overview/vault/compute/plugins/settings/tower/analytics，dashboard.views.js:148）；4 类抽屉（vault-drawer 网页托管发布、article-syndicate-drawer 社媒分发、plugin-drawer、review-drawer 翻译校对）；主题系统 tokens.css 208 变量 + light 覆盖。
- **治理**：SOP 五法典+V10.2 宪法；pre-commit 4 检查；exemptions.yaml 56 文件；CI 两条 workflow（deploy + sentinel-check 自愈）。
- **扫描发现的治理缺口**：`dashboard.imprints.js`（1104 行）与 `ui/modals.js`（634 行）超 SOP-02 500 行硬红线且**不在豁免名单**；`core/pressing、newsroom、submission` 与 `adapters/storage` 为空占位目录；README.zh-CN.md:68 仍写 CC BY-NC 4.0 但 LICENSE 已改 PolyForm Noncommercial（文案不一致）。

**缺口 2+3 已修复（4950d32）**：
- 许可文案统一：README.md/README.zh-CN.md 徽章+开源许可段落、CONTRIBUTING.zh-CN.md:41 贡献者约定，全部从 CC BY-NC 4.0 改为 PolyForm Noncommercial 1.0.0；5 处 `file:///Volumes/...` 本机绝对路径死链改 `./docs/legal/` 相对路径（6 个目标文件已验证存在）；商业授权段补根 COMMERCIAL_LICENSE.md 链接与 wqbyc@msn.com。
- 空占位目录删除：4 个目录仅含 .DS_Store（git 未跟踪、零 import 引用），本地直接删除无需提交；`core/storage` 含 audit_ledger.db 数据文件，不在授权范围，保留。
- **遗留内容级许可**（未动，属发布内容设定非软件许可）：`system.render.js:118` frontmatter 默认 CC BY-NC-SA 4.0；imprints 演示站 about 页 CC BY-NC 4.0（生成产物，改了会被重建覆盖）。