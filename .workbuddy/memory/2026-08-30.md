# 2026-08-30 工作日志

**遗留运行时账本清理（fb25777）**：
- `core/storage/audit_ledger.db`（SQLite，359 条引擎点火/AI 事务审计记录，2026-04 时代）是 V50.5 账本物理隔离重构前的遗留产物，活跃账本早已迁至 `imprints/<id>/metadata/core/audit.db`，全仓无代码引用旧路径。
- 问题：该 db 在 gitignore `*.db` 规则之前已提交，gitignore 不解除已跟踪 → 每个克隆者携带 90KB 运行时数据，违背"运行时数据不进仓库"治理原则。
- 处理：`git rm --cached` 解除跟踪 + 删除本地文件；删除前备份至 `artifacts/backups/audit_ledger_legacy_20260830.db`（gitignored 不进仓库）。
- **重要**：`core/storage/.master.key` 是 SecretManager（`core/governance/secret_manager.py:20`）的 Fernet 主密钥，**活跃使用，绝不可删**，目录为此保留。core/storage 目录现在只剩这个密钥文件。

**商用发布就绪度核查（00:09）**：
- ✅ 测试套件全绿：**469 passed**，33 秒，仅 1 个 Pydantic `dict()` 弃用警告（V3 前需换 `model_dump`）。
- ✅ requirements.txt + pyproject.toml 均在；EULA.md 无 CC BY-NC 残留；config.local.yaml 被 gitignore。
- ❌ **版本号三方不一致**：README.md 徽章 V50.3 / README.zh-CN.md 徽章 v11.2 / CHANGELOG.md 最新 v6.4.0（2026-06-05，近 3 个月未记）。
- ⚠️ SecretManager 加密体系"空转"：全仓 enc: 字段=0，config.local.yaml 2 个 api_key 均为本地模型（lmstudio/ollama）占位符——基础设施就绪但从未加密过真实凭据；换机器 .master.key 会重新生成（enc 数据单机有效，当前无影响）。

**P0 版本号统一（6082a55）**：
- 版本号真理源 = `core/__init__.py::__version__ = "50.3"`（`__edition__ = "Industrial-Sovereignty (工业主权版)"`），终端 `plenipes.py --version` 显示 `Illacme-plenipes v50.3 (Industrial-Sovereignty (工业主权版))`。
- 修复四方不一致：README.zh-CN 徽章 v11.2→v50.3；dashboard 页脚 `footer.template.js:60` V74.0_STABLE→v50.3_STABLE；CHANGELOG 补记 `[v50.3-Industrial-Sovereignty] - 2026-08-30`（汇总 6/5 以来 246 提交，5 主题域，全部映射真实 git 提交）；README.md 徽章本就是 V50.3 无需改。
- CHANGELOG 头部加对齐说明：此后版本号跟 `core/__init__.__version__`，历史条目保留原始编号。发版 SOP：改版本号时改 `core/__init__.py` + README 双徽章 + 页脚 + CHANGELOG 四处。

**第五处版本号残留（用户指出，0e81beb）**：激活授权页「出版引擎版本」徽章 v11.2 有两层来源——后端 `core/governance/license_guard.py:288` 硬编码（`/api/governance/license/info` 实测返回）+ 前端 `license.templates.js:51` 静态占位符。已修：后端接 `from core import __version__`（f"v{__version__}"，升版本自动跟随），前端占位符同步 v50.3。29 个 license 测试全绿。**发版 SOP 更新为五处**：`core/__init__.py`、README 双徽章、dashboard 页脚、license 徽章（已自动化）、CHANGELOG。注意：运行中的 API 服务需重启才返回新版本号。

**第六处版本号残留（用户追问终端 🛰️ Release，33e5414）**：终端启动横幅 `🛰️ Release : V24.0` 来自 `core/ui/handlers/status_handlers.py:93` 的 `quick_banner()`（plenipes.py:72 调用，超早期自举横幅）。根因：同函数 `ael_iter_id="V50.3_Final"` 已半改，唯独 `version="V24.0"` 漏改——半改遗漏。已修：加 `from core import __version__` 并 `version=f"V{__version__}"`，与 --version 及授权页统一。smoke test 渲染到 buffer 验证输出 `🛰️ Release : V50.3`，无早期导入/循环导入失败（miniconda python 运行时）。grep 复核：其余 V24.0/V11.2/V74.0 命中均为代码注释迭代标记、运行时 pulse_*.json 状态文件、历史日志、node_modules，非用户可见发布号。**发版 SOP 补充为六处**：新增「终端 quick_banner 横幅（已自动化接 __version__）」。早期导入安全：core/__init__ 先 import 子模块再设 __version__，license_guard 已同款修法可证无环。

**横幅 Edition 行去重（用户指两行版本号不合适，9e00c47）**：用户指出横幅有「Release: V50.3」+「Edition: V50.3_Final」两行版本号样式冗余。根因：Edition 行原接 `ael_iter_id="V50.3_Final"`，而 `ael_iter_id` 实为 AEL Lineage 运行时审计追踪 ID（`core/utils/tracing.py` 的 contextvars Trace-ID，用于每次发布溯源），并非产品版本；代码把它误当「版本代号」硬编码，与 Release 行构成重复版本展示。已修：`handle_banner` 参数 `ael_iter_id`→`edition`，值接 `core.__edition__`（"Industrial-Sovereignty (工业主权版)"）；`quick_banner` 与 `mediator.py:56` 转发同步改名/改值。现横幅：Release=版本号(V50.3) / Edition=版本代号(Industrial-Sovereignty 工业主权版)，无重复版本行。buffer 渲染断言验证通过。

**发布预览流水线步骤条白天模式（用户截图指不理想，c2c3e1d）**：用户截图反馈「极速发布预览流水线」弹窗的 4 步工作流步骤条在白天模式显示不理想——`.preview-stepper-bar` / `.step-item` / `.step-arrow` 在 `dashboard.components.css:386-427` 有基线样式但**完全没有白天覆盖**（同项目 `.wiz-step` 在 `theme.light.overrides.css:202-225` 有专属覆盖）。具体问题：1) 条背景硬编码 `rgba(0,0,0,0.4)` 在白底模态上叠出灰黑条；2) 三态（全待办/进行/完成）仅靠文字色和 text-shadow 发光区分——发光在白底几乎不可见，4 步平铺无视觉层次；3) 箭头 `opacity: 0.4` + `--text-dim` 几乎看不见。修法（按用户选的 A 方案「完整修法」）：在 `theme.light.overrides.css:268-309` 新增 5 块覆盖，参照 `.wiz-step` 白天模式风格给步骤加**圆角胶囊容器**——待办=白底浅灰边 (#e2e8f0)、进行=#0284c7 蓝实底白字、完成=#f0fdf4 浅绿底+深绿字 (#15803d)；箭头颜色加深至 #94a3b8 opacity 0.9；条背景改 #f8fafc 浅灰、去掉 backdrop-filter、加 1px 浅边和小阴影。暗色基线 CSS 不动。Playwright 三场景截图验证（全待办/1完成+2进行/2完成+3进行）状态一目了然，胶囊+三色清晰区分。
