# 实施任务清单

- [x] 在 `tests/autonomous_simulation.py` 的中段切入 ADMI v2.0 测试流。
- [x] 编写写入伪造的 `vision_test.jpg` 和 Markdown 文档以诱发解析流。
- [x] [V16.6 补丁] 修复文件写入后 `VaultIndexer` 与影子搜寻路径不一致的灾难性 Bug。
- [x] 注入 `Resonance 补丁` 的全量配置：覆盖 `multilingual_alt=True`。
- [x] 建立完备的 Mock `side_effect` 路由用于处理 `describe_image` 及解蔽符重铸。
- [x] 对账本级 Alt 双语字典矩阵断言，对最终 `en` 输出中的物理回填文本断言。
