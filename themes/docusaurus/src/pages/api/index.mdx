---
title: Index
description: 掌握 Illacme Plenipes 引擎核心协议，优化渲染、编译和 AI 调度！了解 `ai_sync` 指令，提升效率，避免 API 风暴。
keywords:
- Illacme Plenipes
- 引擎协议
- ai_sync
- 渲染优化
- 编译优化
- AI 调度
- 技术文章
author: Eason
date: &id001 2026-04-10 10:39:08.516511+08:00
template: splash
ai_sync: true
last_update:
  date: *id001
  author: Eason
---



本域提供 `Illacme Plenipes` 引擎在渲染、编译和 AI 调度周期内所需的所有底层接口声明。遵循“契约即代码”的原则，所有规范均具有强制执行效力。

## 📄 Frontmatter 核心协议

在每个 `.md` / `.mdx` 文件的顶部，通过 YAML 注入的元数据控制着单篇文档的生命周期。

### 状态管控指令

引擎特有的运行时干预指令，享有最高执行优先级。

#### `ai_sync` <span style={{backgroundColor: "#2e8555", color: "#fff", padding: "2px 6px", borderRadius: "4px", fontSize: "0.8em", verticalAlign: "middle"}}>boolean</span> <span style={{backgroundColor: "#2e8555", color: "#fff", padding: "2px 6px", borderRadius: "4px", fontSize: "0.8em", verticalAlign: "middle"}}>默认: true</span>

大模型算力通信锁。专为极客打造的成本控制中枢。

:::danger 架构警告
  当你处于高频修正错别字，或编写复杂的 HTML/MDX 组件结构时，**必须**将此项设为 `false`。这能彻底斩断看门狗对该文件的大模型并发请求，防止触发 API 风暴或破坏 DOM 结构。
:::

```yaml
---
title: 我的硬核技术文章
ai_sync: false
---